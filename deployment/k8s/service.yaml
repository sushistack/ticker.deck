apiVersion: v1
kind: Service
metadata:
  name: tickerdeck
  labels:
    app.kubernetes.io/name: tickerdeck
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: tickerdeck
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
